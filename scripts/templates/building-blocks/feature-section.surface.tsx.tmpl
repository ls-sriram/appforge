// {{FEATURE}}-{{SECTION}}.surface — visual container surface.
// Calls useViewport() if layout varies by size. Uses design tokens for color/spacing.
// Accepts data props only — no hooks, no viewmodel refs, no children slots.

import React from "react";
import { StyleSheet, View } from "react-native";
import { useTheme } from "../../../../theme/ThemeProvider";
import { useViewport } from "../../../../theme/Viewport";

interface {{PASCAL_FEATURE}}{{PASCAL_SECTION}}SurfaceProps {
  // TODO: add data props
}

export function {{PASCAL_FEATURE}}{{PASCAL_SECTION}}Surface({}: {{PASCAL_FEATURE}}{{PASCAL_SECTION}}SurfaceProps) {
  const t = useTheme();
  const { isDesktop } = useViewport();

  return (
    <View style={[s.root, { backgroundColor: t.colors.surface }]}>
      {/* TODO */}
    </View>
  );
}

const s = StyleSheet.create({
  root: { borderRadius: 12, padding: 16 },
});
