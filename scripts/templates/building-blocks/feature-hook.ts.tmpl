// {{FEATURE}}.hook — raw data subscription only, no derivation
// Reads from stores or external sources and returns unprocessed state.
// Derivation and actions belong in {{FEATURE}}.viewmodel.ts.

export function use{{PASCAL_FEATURE}}Data() {
  return {};
}
