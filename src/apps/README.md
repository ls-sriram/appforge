# App-Specific Frontend Code

`src/apps/` contains application-specific frontend modules.

- `example-app/`: reference app scaffold with onboarding, profile, and session-backed home flows.

Shared platform code should remain outside this folder unless it is truly app-specific.
